# Runtime Data

The web app writes local SQLite booking/session data here by default:

```text
bookings.sqlite3
```

Generated database files are ignored by Git. They are local proof-of-concept
runtime artifacts, not source data.
