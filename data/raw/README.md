# Raw Data

Place or clone the lecturer-provided MultiWOZ repository locally, then point the
preprocessing script at it:

```powershell
python scripts/prepare_multiwoz.py --multiwoz-path C:\path\to\multiwoz
```

The script searches for `restaurant_db.json` in common MultiWOZ locations. The
full raw dataset is intentionally not committed to this repository.

